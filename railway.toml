[build]
builder = "nixpacks"
buildCommand = "apt-get update && apt-get install -y python3 python3-pip && cd ml_services && python3 -m pip install --prefer-binary --no-cache-dir -r requirements.txt"

[deploy]
startCommand = "cd ml_services && uvicorn app:app --host 0.0.0.0 --port $PORT"