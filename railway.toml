[build]
builder = "nixpacks"
buildCommand = "pip install -r ml_services/requirements.txt"

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[deploy]
startCommand = "cd ml_services && python -m uvicorn app:app --host 0.0.0.0 --port $PORT"